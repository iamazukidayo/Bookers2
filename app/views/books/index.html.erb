
<%= render 'users/userinfo',user: @users %>
<%= render 'newbook', book: @book %>

  <div class="row">
<div class="col-md-8 offset-md-1">
<h2>Books</h2>
<table>
  <thead>
      <tr>

  <th>Title</th>
  

  <th>Opinion<th>
  <th></th>

  </tr>
  </thead>

 <tbody>
   <% @books.each do |book| %>
  <tr>
    
    <td><%= link_to  book.title, book_path(book.id) %></td>
    <td><%= book.body %></td>
 </tbody>


 <% end %>

<h1>User info</h1>
<%= image_tag @user.get_profile_image(100, 100) %>

<h5>name</h5>
<%= @user.name %>

<h5>introduction</h5>
<%= @user.introduction %>


<%= link_to "設定", edit_user_path(current_user.id)  %>







